<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SkillFolioFinal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Nonaktifkan animasi default Android agar custom transition berjalan smooth -->
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="Theme.SkillFolioFinal" parent="Base.Theme.SkillFolioFinal" />

    <style name="CustomActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/primary_yellow</item>
    </style>

    <!-- Style untuk avatar lingkaran — dipakai di semua top bar -->
    <style name="CircleImageStyle" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>